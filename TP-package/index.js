const { toSlug, titleCase  } = require("./strings");
const { sortArray, randomArrayElement } = require("./arrays");

module.exports = {
    toSlug,
    titleCase,
    sortArray,
    randomArrayElement
};